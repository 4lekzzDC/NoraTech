import SistemaEmConstrucao from '../../pages/SistemaEmConstrucao';

// Placeholder do módulo Conciliador de Extratos.
// Substitua por implementação real quando começar a migrar do Autonomy.

export default function ConciliadorExtratosPage() {
  return <SistemaEmConstrucao slug="conciliador-extratos" />;
}
