import SistemaEmConstrucao from '../../pages/SistemaEmConstrucao';

// Placeholder do módulo Conciliador de Fornecedores.
// Substitua por implementação real quando começar a migrar do Autonomy.

export default function ConciliadorFornecedoresPage() {
  return <SistemaEmConstrucao slug="conciliador-fornecedores" />;
}
