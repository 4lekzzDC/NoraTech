import SistemaEmConstrucao from '../../pages/SistemaEmConstrucao';

// Placeholder do módulo Gestão de Clientes (CRM).
// Substitua por implementação real quando começar a migrar do Autonomy.

export default function GestaoClientesPage() {
  return <SistemaEmConstrucao slug="gestao-clientes" />;
}
